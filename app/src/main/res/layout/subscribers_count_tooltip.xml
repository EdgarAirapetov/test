<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tooltipRootView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:background="@drawable/new_tooltip_main_background"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/tooltipIcon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="start|center_vertical"
            android:layout_marginStart="12dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:src="@drawable/ic_user_subscribers_tooltip" />

        <TextView
            android:id="@+id/tooltipText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="start|center_vertical"
            android:includeFontPadding="false"
            android:padding="8dp"
            android:text="@string/user_subscribers_tooltip"
            android:textAppearance="@style/TooltipText"
            android:textColor="@color/tooltip_text_color"
            android:textSize="14sp" />

    </LinearLayout>

</FrameLayout>