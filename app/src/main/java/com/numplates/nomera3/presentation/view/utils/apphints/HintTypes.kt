package com.numplates.nomera3.presentation.view.utils.apphints

enum class HintTypes {

    EMPTY,
    GROUP_CHAT_CREATE,

    // Chat
    RECORD_AUDIO_MESSAGE,
    SEND_AUDIO_MESSAGE,
    AUDIO_MESSAGE_ON_CLICK, // every time

    // Road
    ROAD_FILTER,
    ROAD_NEW_POST,

    // Calls - every time
    CALL_SPEAKER_ON,
    CALL_SPEAKER_OFF,
    CALL_MICROPHONE_ON,
    CALL_MICROPHONE_OFF,

    // Map
    MAP_FILTER,
    MAP_FILTER_V2,

    // Account new function
    ACCOUNT_NEW_FUNCTION,

    ABOUT_UNIQUE_NAME_USER_INFO
}